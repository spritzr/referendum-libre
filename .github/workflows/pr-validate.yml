name: PR Validate

# Required status check on PRs targeting `develop`. Runs the same checks
# CONTRIBUTING.md asks contributors to run locally before opening a PR,
# so drift gets caught at PR time rather than in a maintainer's review.
#
# Note: `tsc --noEmit` is intentionally NOT gated here. The repo currently
# has pre-existing type errors in QA/diagnostic screens and exported
# reference folders that block a clean tsc run. Add a separate workflow
# (or extend this one) once those are cleaned up.

on:
  pull_request:
    branches: [develop]

# Default GITHUB_TOKEN scopes are read-write across most APIs on private
# repos. Pin the workflow to read-only so a compromised third-party action
# can't push, open issues, or rewrite branches via the chain of trust.
# Jobs that need to write (cache, comment, release) must add the specific
# permission they need at the job level.
permissions: read-all

concurrency:
  group: pr-validate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Setup pnpm
        # Version comes from the `packageManager` field in package.json.
        uses: pnpm/action-setup@v4
      - name: Setup Node 20
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: pnpm
      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile
      - name: ESLint
        run: pnpm lint

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 20
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: pnpm
      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile
      - name: Jest
        run: pnpm test --ci

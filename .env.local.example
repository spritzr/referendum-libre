# Template. Fill and copy to .env.local (secrets, gitignored)
# Copy to .env.local and fill in. Do NOT commit real values.

# --- Dev-mode auto-fill (optional) ---
# Pre-fills the CAN / MRZ fields on the dev-only can-scan and passport-test
# screens so you don't have to retype test document values every reload.
# Leave unset for no auto-fill. These are EXPO_PUBLIC_* so they ship in the
# JS bundle — never put a real person's document data here.
EXPO_PUBLIC_DEV_VOTER_CAN=
EXPO_PUBLIC_DEV_MRZ_DOCUMENT_NO=
EXPO_PUBLIC_DEV_MRZ_BIRTH_DATE=
EXPO_PUBLIC_DEV_MRZ_EXPIRY_DATE=

# Org App Store Connect account-holder Apple ID (replaces the old personal email).
EXPO_APPLE_ID=

# --- Auth: pick ONE of the two below ---

# Option A: app-specific password (appleid.apple.com -> Sign-In & Security -> App-Specific Passwords)
EXPO_APPLE_APP_SPECIFIC_PASSWORD=

# Option B: App Store Connect API key (recommended for CI). Generate under the
# org: App Store Connect -> Users and Access -> Integrations -> App Store Connect API.
# Download the .p8 once and keep it out of git (./secrets is gitignored).
EXPO_ASC_API_KEY_PATH=./secrets/AuthKey_XXXXXX.p8
EXPO_ASC_KEY_ID=
EXPO_ASC_ISSUER_ID=
